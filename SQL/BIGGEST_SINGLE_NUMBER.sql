CREATE DATABASE BIGGEST_SINGLE_NUMBER;

USE BIGGEST_SINGLE_NUMBER;

CREATE TABLE MyNumbers (
    num INT
);

INSERT INTO MyNumbers (num) VALUES
(8),
(8),
(3),
(3),
(1),
(4),
(5),
(6);

SELECT MAX(NUM) AS NUM FROM (
SELECT NUM FROM MYNUMBERS GROUP BY NUM HAVING COUNT(NUM)=1
) AS TEMP